// function luasPersegiPanjang(panjang, lebar) {
//   return panjang * lebar;
// }

// one-liner lover
const luasPersegiPanjang = (panjang, lebar) => panjang * lebar;
console.log(luasPersegiPanjang(5, 10)); // 50
